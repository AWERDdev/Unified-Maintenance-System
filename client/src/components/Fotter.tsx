'use client'
import {ROUTES} from "@/Types/Routing"
import { useRouter } from "next/navigation";

export const Fotter = () =>{
      const router = useRouter()
    return(
        <>

        </>
    )
}